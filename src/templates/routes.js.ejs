const express = require('express');
const routes = express.Router();
// Importação de Controller
const ExampleControlle = require('./controllers/ExampleController');



// Rota de Users
routes.get('/example', ExampleControlle.index);

module.exports = routes;